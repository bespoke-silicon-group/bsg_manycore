TOOL_ROOT=/mnt/bsg/diskbits/neilryan
LLVM_DIR=$(TOOL_ROOT)/llvm/llvm-install
CC=$(LLVM_DIR)/bin/clang

PASS_FILE=~/llvm-pass-skeleton/build/skeleton/libSkeletonPass.so
INC_PASS=-Xclang -load -Xclang $(PASS_FILE)
all:
	$(CC) -v main.c

clean:
	rm -f a.out
